# 📋 تعليمات إضافة حقل صور الفئات

## المشكلة
قاعدة البيانات الحالية لا تحتوي على حقل `image_url` في جدول `categories`، مما يسبب خطأ عند محاولة حفظ صور الفئات.

## الحل 🔧

### الطريقة الأولى: عبر Supabase Dashboard (موصى بها)

1. اذهب إلى [Supabase Dashboard](https://app.supabase.com)
2. اختر مشروعك
3. اذهب إلى **SQL Editor** من القائمة الجانبية
4. انسخ والصق الكود التالي:
```sql
ALTER TABLE categories 
ADD COLUMN IF NOT EXISTS image_url TEXT;

COMMENT ON COLUMN categories.image_url IS 'URL of the category image';
```
5. انقر على **RUN** لتنفيذ الكود
6. انتظر رسالة التأكيد

### الطريقة الثانية: عبر ملف SQL

1. افتح الملف `add_category_image_column.sql` في المحرر
2. انسخ محتواه
3. اذهب إلى Supabase Dashboard → SQL Editor
4. الصق الكود ونفذه

## التحقق ✅

بعد التنفيذ، يمكنك التحقق من إضافة الحقل:

1. اذهب إلى **Table Editor** في Supabase Dashboard
2. اختر جدول `categories`
3. يجب أن ترى عمود جديد اسمه `image_url`

## بعد الإضافة 🎉

بعد إضافة الحقل بنجاح:
- يمكنك رفع صور الفئات من صفحة admin.html
- ستظهر الصور في قائمة الطعام (menu.html)
- كل فئة يمكن أن يكون لها صورة مميزة

## ملاحظات

- الحقل اختياري (يمكن أن يكون null)
- يدعم روابط الصور الخارجية
- الحجم الأقصى للصورة: 5MB
